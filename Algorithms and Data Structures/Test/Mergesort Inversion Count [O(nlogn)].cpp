#include "../Implementations/Mergesort Inversion Count [O(nlogn)].h"

/*template<typename T>
struct Func
{
	void operator()(const T& t)
	{
		cout << "yo\n";
	}
};*/

void go()
{
	uli a;
	puli b;
	tuple<uli, uli, uli> c;
	array<uli, 4> d;
	vuli e(5);
	duli f(6);
	l2uli g(7);
	l1uli h(8);

	read(tie(a, b, c, d, e, f, g, h));
	write(tie(a, b, c, d, e, f, g, h), '\n', ' ');

	//vector<string> ms{ "hi", "hello", "hi", "hi!" };
	//auto tup = make_tuple("hello", 4, vuli{ 2, 3, 4 }, make_tuple("yo", "howdy", suli{ 1, 1, 4 }, make_tuple(make_tuple(make_pair(4, 5)))));
	//multiset<string> ms{ "hi", "hello", "hi", "hi!" };
	//write(tup, 'a', 'b', 'c', 'd', 'e', 'f', 'g');
	//vector<int> l(5);
	//tuple<int, int, int, int, int> l;
	//read(l);
	//write(l, make_tuple(5));
	//for (auto i : l) cout << i << endl;
	//tuple<string, int, char> tup;
	//read(tup);
	//cout << get<0>(tup) << endl << get<1>(tup) << endl << get<2>(tup);
	//tuple<string, int, vector<int>> tup{ "hi", 5, vli{2, 4, 6} };
	//for_each<Func>(tup);
	//write(vli{ 2, 4, 6 }, 4);
	//write(tup, make_tuple("---", 4));
	//return;
	//vuli vec(read<uli>());
	//read(ALL(vec));
	//cout << mergeSortInversionCount(ALL(vec)) << endl;
	//write(ALL(vec));
}